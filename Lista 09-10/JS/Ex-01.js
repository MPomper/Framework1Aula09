// JavaScript source code
function NamesUsers(quantidade) {
    NamesArray = new Array();
    for (var i = 0; i < quantidade; i++) {
        NamesArray = prompt("Digite o " + (i + 1) + " nome: ");
    }
}